// cabeçalhos e structs

#include <stdio.h>
#include <stdlib.h>

// Estrutura para armazenar um elemento da matriz
typedef struct {
    int i;     // Linha
    int j;     // Coluna
    double valor;  // Valor do elemento
} ElementoMatriz; 